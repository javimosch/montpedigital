version: '3.1'

services:
  builder:
    restart: "no"
    build: ./
    container_name: montech_builder
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app
    entrypoint: 
      - yarn
      - start